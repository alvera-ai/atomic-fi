# Provider selection — must be "openai" or "anthropic".
LLM_PROVIDER=openai

# Optional model override. Defaults: gpt-4o-mini / claude-sonnet-4-6.
# LLM_MODEL=

# Set the matching one for your LLM_PROVIDER.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

PORT=4111
