VITE_ATOMIC_FI_API_KEY=replace-me

# CopilotKit v2 runtime URL — the copilot-runtime sidecar that brokers LLM
# calls (docs/copilot-architecture.md §8, docs/adr/ADR-002). Unset, the
# editor falls back to the same-origin /api/copilotkit path.
VITE_COPILOT_RUNTIME_URL=http://localhost:4242/api/copilotkit
