# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ephemeral bench working state — sharded NDJSON inputs + per-source
# subprocess proofs that get folded into the single committed report
# under benchmarks/. Regenerated from a fixed RNG seed on every run.
/.bench-work/

# Ignore package tarball (built via "mix hex.build").
phoenix_template_server-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# pnpm workspace
node_modules/
.pnpm-store/

# Per-runId integration-test state — never commit.
/integration-tests/vitest-state/

# Bootstrap credentials dumped by `mix atomic_fi.dump_bootstrap_creds` for E2E tests.
/priv/repo/.bootstrap_creds.json

# Local IDE / tooling caches — keep .claude/ tracked (intentional), ignore the rest.
/.vscode/
/.playwright-cli/
**/.playwright-cli/
# Exploratory Playwright "walk" scratchpads — manual flow discovery before
# codifying into the real e2e/ specs. Screenshots, traces, throwaway
# walker specs. Regenerated per walk; never committed.
**/.playwright-sessions/
# Playwright run artefacts at the repo root (root playwright.config.ts
# writes here). The per-app outputs are already covered by the
# `example-apps/*/test-results/` rule above.
/test-results/
/playwright-report/
# Per-session runtime lock from Claude's scheduled-task harness — pid + sessionId.
/.claude/scheduled_tasks.lock

# Dev secrets (API keys) — never commit.
/config/dev.secret.exs
/.env

# Local test overrides (LLM transport → Ollama). No secrets, but
# per-developer — not committed.
/config/test.secret.exs

# macOS Finder metadata.
**/.DS_Store

# Example-app Vite build output — produced by `make server` watchers
# into priv/static/demo/<app>/, served by Plug.Static. Each example
# app's source lives under example-apps/<app>/; the dist is throwaway.
/priv/static/demo/onboarding-flow/
/priv/static/demo/atomic-fi-jdm-editor/
/priv/static/demo/lotus-embed/

# ZenRule runtime rules — demo subdirs only. Source of truth lives at
# /zen_rules/ at the repo root; `make run-backing-services` copies them
# into these locations (which gorules/agent bind-mounts read-only). Tests
# + the JDM editor's save_rule action write into these dirs at runtime —
# fine, because they're throwaway and repopulated from /zen_rules/ on the
# next make run. Same pattern as the Vite build output above.
# (priv/zenrule/test-fixtures-* are NOT ignored — they're committed
# ExUnit test fixtures used by test/atomic_fi/rule_engine/default_test.exs.)
/priv/zenrule/onboarding/
/priv/zenrule/transaction-screening/

# Example-app local dev secrets (Vite reads .env files).
/example-apps/*/.env
/example-apps/*/.env.local

# Playwright run artefacts (screenshots, traces) — regenerated per run.
/example-apps/*/test-results/
/example-apps/*/playwright-report/


# Hydrated Watchman data dir (make hydrate-watchlists); source of truth is /watchlists
priv/watchman-data/
