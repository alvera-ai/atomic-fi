meta {
  name: 008 — Create receiver payment account (USD)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/payment-accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authBearer}}
}

headers {
  content-type: application/json
  accept: application/json
}

script:pre-request {
  bru.setVar("externalId", `IDPEP-PA-RECV-${Date.now()}-${Math.floor(Math.random() * 1e6)}`);
}

body:json {
  {
    "external_id": "{{externalId}}",
    "account_type": "bank_account",
    "currency": "USD",
    "account_holder_id": "{{receiverAhId}}",
    "tenant_id": "{{tenantId}}"
  }
}

script:post-response {
  bru.setEnvVar("receiverPaId", res.body.id);
}

assert {
  res.status: eq 201
}
