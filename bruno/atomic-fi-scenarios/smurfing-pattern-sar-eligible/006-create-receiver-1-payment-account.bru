meta {
  name: 006 — Create receiver #1 payment account
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/payment-accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authBearer}}
}

headers {
  content-type: application/json
  accept: application/json
}

script:pre-request {
  bru.setVar("rPaExternalId", `SMURF-PA-RCV-1-${Date.now()}-${Math.floor(Math.random() * 1e6)}`);
}

body:json {
  {
    "external_id": "{{rPaExternalId}}",
    "account_type": "wallet",
    "currency": "USD",
    "account_holder_id": "{{receiverAhId1}}",
    "tenant_id": "{{tenantId}}"
  }
}

script:post-response {
  bru.setEnvVar("receiverPaId1", res.body.id);
}

assert {
  res.status: eq 201
}
