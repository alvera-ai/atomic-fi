{
  "contentType": "application/vnd.gorules.decision",
  "nodes": [
    {
      "id": "request",
      "type": "inputNode",
      "name": "Request",
      "position": { "x": 100, "y": 160 }
    },
    {
      "id": "decide",
      "type": "expressionNode",
      "name": "De Minimis",
      "position": { "x": 380, "y": 160 },
      "content": {
        "expressions": [
          {
            "id": "out_ledger_accounts",
            "key": "ledger_accounts",
            "value": "creditor_payment_account == null or debtor_payment_account == null ? {} : (transaction.transaction_type == 'internal_transfer' ? (len(filter(creditor_payment_account.las, # .regime == 'internal_transfer')) > 0 and len(filter(debtor_payment_account.las, # .regime == 'internal_transfer')) > 0 ? (creditor_payment_account.account_holder.kyc_status == 'approved' ? {[filter(creditor_payment_account.las, # .regime == 'internal_transfer')[0].id]: { reason: 'stablecoin_de_minimis' }, [filter(debtor_payment_account.las, # .regime == 'internal_transfer')[0].id]: { reason: 'stablecoin_de_minimis' }} : {[filter(creditor_payment_account.las, # .regime == 'internal_transfer')[0].id]: { daily_debit_cap: 0, daily_credit_cap: 0, reason: 'stablecoin_block_unverified', block_reason: 'stablecoin_block_unverified', is_blocked: true }, [filter(debtor_payment_account.las, # .regime == 'internal_transfer')[0].id]: { daily_debit_cap: 0, daily_credit_cap: 0, reason: 'stablecoin_block_unverified', block_reason: 'stablecoin_block_unverified', is_blocked: true }}) : {}) : (transaction.transaction_type == 'credit_transfer' ? (len(filter(creditor_payment_account.las, # .regime == 'ach')) > 0 and len(filter(debtor_payment_account.las, # .regime == 'ach')) > 0 ? {[filter(creditor_payment_account.las, # .regime == 'ach')[0].id]: { daily_debit_cap: 10000, daily_credit_cap: 10000, weekly_debit_cap: 50000, weekly_credit_cap: 50000, monthly_debit_cap: 100000, monthly_credit_cap: 100000, yearly_debit_cap: 500000, yearly_credit_cap: 500000, reason: 'ach_de_minimis' }, [filter(debtor_payment_account.las, # .regime == 'ach')[0].id]: { daily_debit_cap: 10000, daily_credit_cap: 10000, weekly_debit_cap: 50000, weekly_credit_cap: 50000, monthly_debit_cap: 100000, monthly_credit_cap: 100000, yearly_debit_cap: 500000, yearly_credit_cap: 500000, reason: 'ach_de_minimis' }} : {}) : {}))"
          }
        ]
      }
    },
    {
      "id": "response",
      "type": "outputNode",
      "name": "Response",
      "position": { "x": 700, "y": 160 }
    }
  ],
  "edges": [
    { "id": "edge_request_decide", "type": "edge", "sourceId": "request", "targetId": "decide" },
    { "id": "edge_decide_response", "type": "edge", "sourceId": "decide", "targetId": "response" }
  ]
}
